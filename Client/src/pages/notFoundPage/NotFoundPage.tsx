import "../homePage/home.css";

const NotFoundPage = (): JSX.Element => {
  return (
    <div className="server-error">
      Oopsss. You're not supposed to be here :)
    </div>
  );
};

export default NotFoundPage;
